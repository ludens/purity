# Agent notes

- 티스토리 스킨. 치환자는 `.agents/skills/tistory-skin`만 본다. 문서에 없는 이름을 만들지 않는다.
- `index.xml`을 바꾸면 티스토리에 올릴 때 스킨 설정이 전부 초기화된다. README에도 적혀 있지만 놓치기 쉽다.
- 데모 https://puredemo.tistory.com 은 zip을 다시 올려야 로컬과 같아진다. 라이브 HTML만 보고 로컬이 반영됐다고 단정하지 않는다.
- `[##_comment_group_##]`는 `<s_rp>` 안, `[##_guestbook_group_##]`는 `<s_guest>` 안에 있어야 치환된다.
- 화면 UI 점검은 `.agents/skills/purity-review` (`/purity-review`).
